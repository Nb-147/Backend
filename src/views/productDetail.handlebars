<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{product.title}} Details</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1>{{product.title}} Details</h1>

    <ul id="indexList">
        <li class="wide-card"> 
            <strong>{{product.title}}</strong> 
            <hr> 
            U$S {{product.price}}
            <br>
            <p><strong>Category:</strong> {{product.category}}</p>
            <p><strong>Description:</strong> {{product.description}}</p>
            <p><strong>Stock:</strong> {{product.stock}}</p>
            <p><strong>Available:</strong> {{#if product.status}} Yes {{else}} No {{/if}}</p>

            {{#if cartId}}
            <form action="/api/carts/{{cartId}}/products/{{product._id}}" method="POST">
                <input type="hidden" name="quantity" value="1">
                <button type="submit">Add to Cart</button>
            </form>
            {{else}}
            <p><strong>Error:</strong> No cart available. Please create a cart first.</p>
            {{/if}}
        </li>
    </ul>

    <br>
    <button class="returToHome" onclick="location.href='/api/products'">Back to Products List</button>
</body>

</html>